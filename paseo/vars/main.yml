---
# Internal — not meant to be overridden per host.
paseo_node_packages:
  Gentoo: net-libs/nodejs
  Debian:
    - nodejs
    - npm
  RedHat:            # Fedora/RHEL: npm ships as a separate package (nodejs-npm)
    - nodejs
    - nodejs-npm
  Archlinux:
    - nodejs
    - npm
